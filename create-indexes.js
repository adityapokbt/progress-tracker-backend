require('dotenv').config();

console.log('Firestore indexes are managed in the Firebase console.');
console.log('Create composite indexes as needed when Firestore reports missing index errors.');
console.log('Suggested collections: attendance, staff, products, bills, payroll, leave.');

process.exit(0);
